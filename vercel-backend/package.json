{
    "name": "mantra-vercel-backend",
    "version": "1.0.0",
    "description": "Vercel serverless backend for Mantra app - Stellar crypto payments",
    "license": "UNLICENSED",
    "private": true,
    "scripts": {
        "dev": "vercel dev",
        "build": "tsc --noEmit",
        "lint": "tsc --noEmit",
        "vercel-build": "echo 'Build complete'",
        "postinstall": "echo 'Dependencies installed'"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.39.0",
        "@stellar/stellar-sdk": "^12.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.10.0",
        "@vercel/node": "^3.0.0",
        "typescript": "^5.3.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}